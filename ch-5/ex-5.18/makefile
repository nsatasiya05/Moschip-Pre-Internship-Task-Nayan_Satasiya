# Makefile for Exercise 5.18 - Make dcl recover from input errors

CC = gcc
CFLAGS = -Wall -Wextra -std=c99

TARGET = ex-5.18

$(TARGET): $(TARGET).c
	$(CC) $(CFLAGS) -o $(TARGET) $(TARGET).c

clean:
	rm -f $(TARGET)
